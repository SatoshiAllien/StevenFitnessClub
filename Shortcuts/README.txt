STEVENFITNESSCLUB — SHORTCUTS
=============================

ON MAC (use these):
  ▶ "Open in Xcode.command"     → opens project in Xcode
  ▶ "Open Project Folder.command" → opens ios folder in Finder

  How: double-click the file. If blocked:
  Right-click → Open → Open

ON WINDOWS:
  ▶ "Open in Xcode (Windows).bat" → opens project folder
  (Xcode only works on Mac — use Mac shortcut to run the app)

PROJECT LOCATION:
  iCloud Drive / StevenFitnessClub / ios / StevenFitnessClub.xcodeproj